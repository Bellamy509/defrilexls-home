import { redirect } from 'next/navigation';

export default function SignInPage() {
  // This is a placeholder page that redirects to home
  // The actual sign-in is handled by the modal in the header
  redirect('/');
}
